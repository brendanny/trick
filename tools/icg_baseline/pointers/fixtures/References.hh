/* PURPOSE: (Legacy reference boundary characterization; not admitted.) */
#ifndef ICG_BASELINE_REFERENCES_HH
#define ICG_BASELINE_REFERENCES_HH
struct ReferenceModel {
    int& count;
    double& position;
};
#endif
